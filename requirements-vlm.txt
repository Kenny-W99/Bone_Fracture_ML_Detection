# VLM baseline add-on dependencies
# Existing project likely already has pandas, pillow, scikit-learn, matplotlib, tqdm.
openai>=1.0.0
google-genai>=0.8.0
anthropic>=0.40.0
pandas
pillow
scikit-learn
matplotlib
tqdm
